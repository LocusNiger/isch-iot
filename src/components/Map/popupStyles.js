const popupContent = {
  display: "flex",
  flexDirection: "column",
  gap: ".3rem",
  padding: ".3rem",
  width: "18.5rem",
}

const popupHead = {
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  gap: "10px",
}

const popupState = {
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
}

const noMargin = {
  margin: "0",
  padding: "0",
}

const detailButton = {
  padding: "4px",
}

const head = {
  display: "flex",
  flexDirection: "column",
  gap: "1",
}

const bold = {
  fontWeight: "700",
}

export {
  popupContent,
  popupHead,
  popupState,
  noMargin,
  detailButton,
  head,
  bold,
}
